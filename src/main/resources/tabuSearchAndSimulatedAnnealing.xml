<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://timefold.ai/xsd/solver" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://timefold.ai/xsd/solver https://timefold.ai/xsd/solver/solver.xsd">
    <!-- Define the model -->
    <solutionClass>org.acme.schooltimetabling.domain.Timetable</solutionClass>
    <entityClass>org.acme.schooltimetabling.domain.Lesson</entityClass>

    <!-- Define the score function -->
    <scoreDirectorFactory>
        <constraintProviderClass>org.acme.schooltimetabling.solver.TimetableConstraintProvider</constraintProviderClass>
    </scoreDirectorFactory>

    <!-- Configure the optimization algorithms (optional) -->
    <termination>
        <hoursSpentLimit>600</hoursSpentLimit>
    </termination>
    <constructionHeuristic>
        <constructionHeuristicType>FIRST_FIT</constructionHeuristicType>
    </constructionHeuristic>
    <localSearch>
        <acceptor>

            <entityTabuRatio>0.02</entityTabuRatio>
            <simulatedAnnealingStartingTemperature>1hard/550soft</simulatedAnnealingStartingTemperature>
        </acceptor>
        <forager>
            <acceptedCountLimit>10</acceptedCountLimit>
        </forager>
<!--        <acceptor>-->
<!--            <stepCountingHillClimbingSize>400</stepCountingHillClimbingSize>-->
<!--        </acceptor>-->
<!--        <forager>-->
<!--            <acceptedCountLimit>11</acceptedCountLimit>-->
<!--        </forager>-->
    </localSearch>
</solver>
